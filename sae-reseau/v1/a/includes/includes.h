#include <stdio.h>      // Fonctions d'entrée/sortie comme printf, scanf, etc.
#include <stdlib.h>     // Gestion de la mémoire (malloc, free, exit, etc.)
#include <string.h>     // Manipulation des chaînes de caractères (strlen, strcpy, strcmp, etc.)
#include <stdint.h>     // Types à taille fixe (int32_t, uint8_t, etc.)
#include <stdbool.h>    // Booléens en C (true, false)
#include <assert.h>     // Macros pour assertions (assert)
#include <ctype.h>      // Manipulation de caractères (isdigit, isalpha, tolower, etc.)
#include <errno.h>      // Codes d'erreur (errno) et gestion des erreurs (perror)
#include <math.h>       // Fonctions mathématiques (sin, cos, sqrt, pow, etc.)
#include <time.h>       // Gestion du temps (time, clock, strftime, etc.)
#include <unistd.h> 
#include <sys/wait.h>
#include <sys/mman.h>
#include <signal.h>
